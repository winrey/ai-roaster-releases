# AI Roaster — macOS インストールガイド

[简体中文](INSTALL.md) · [English](INSTALL.en.md) · **日本語**

[製品紹介に戻る](README.ja.md) · [ダウンロードと更新履歴](https://github.com/winrey/ai-roaster-releases/releases)

すべての利用枠を、ひと目で把握。

## インストール

1. [Releases](https://github.com/winrey/ai-roaster-releases/releases) から `YuHuo-AI-Roaster_0.2.6_macOS_universal.dmg` をダウンロードして開きます。
2. インストーラー内のアプリを **Applications（アプリケーション）** にドラッグします。
3. 「アプリケーション」から **AI Roaster** を開きます（中国語のシステムでは **余火** と表示されます）。

Finder と Dock の名称は macOS の言語設定で決まり、システムがアプリを再認識した後に更新される場合があります。アプリ本体の実際のファイル名は `余火 · AI Roaster.app` のままで、手動で変更する必要はありません。

旧版を終了してから、新版をインストールして起動してください。アプリ内部の識別子は変わらないため、既存のアカウント、認証情報、設定、履歴を引き続き使用できます。

ユニバーサルインストーラーには Apple シリコンと Intel の両アーキテクチャが含まれます。実際の動作確認は Apple シリコン搭載の macOS 26.1 で行っています。Intel Mac とそれ以前の macOS での実機検証は未実施です。

現在の推奨版は [v0.2.6 正式版](https://github.com/winrey/ai-roaster-releases/releases/tag/v0.2.6)です。署名、公証、検証の結果はこのリリースの `BUILD-INFO.txt` / `NOTARIZATION.json`、ファイルのチェックサムは `SHA256SUMS.txt` を確認してください。初回起動時には、インターネットからダウンロードされたアプリであることを確認する通常のメッセージが表示される場合があります。

開発元を確認できないと表示されたり、macOS に起動を阻止されたりした場合は、まずこのリポジトリの Releases から入手したインストーラーであることを確認し、メッセージ全文を報告してください。他のバージョンの公証状況は、インストーラーに付属するビルド情報を確認してください。

**署名済み・未公証のテストビルド**では、初回起動が macOS に阻止されることがあります。入手元を確認したうえで、「システム設定 → プライバシーとセキュリティ」から「このまま開く」を選び、「開く」で確認できます。このアプリだけの例外を作成する操作で、システム全体のセキュリティ保護を無効にする必要はありません。[Apple の手順](https://support.apple.com/ja-jp/102445)を参照してください。

## ソフトウェア更新

v0.2.5 から、macOS でアプリ内更新を利用できます。アプリメニューの **更新を確認** は公開リリースリポジトリを参照し、既定では正式版のみを確認します。プレリリースを含めることもできます。自動確認と自動ダウンロードは既定で有効です。署名付き更新パッケージがある新版を検出すると、バックグラウンドでダウンロードして署名を検証します。インストールと再起動は、利用者が **再起動して更新** をクリックした時に行います。

自動確認と自動ダウンロードは個別に設定できます。自動確認をオフにすると、バックグラウンドでのダウンロードも停止します。手動での確認とダウンロードは引き続き利用できます。署名付きのアプリ内更新パッケージがないリリースは、公開ページから DMG をダウンロードして手動でインストールできます。

**v0.2.4 以前から更新する場合は、最初に一度だけ v0.2.5 以降を手動でインストールしてください。その後はアプリ内更新を利用できます。**

## 初回利用

- 画面表示は日本語、簡体字中国語、英語に対応し、初期設定ではシステム言語に合わせます。手動で選ぶ場合は、**設定 → 一般 → 言語**で選択し、**設定を保存**をクリックしてください。選択した言語が反映され、次回以降も保持されます。アプリ内の名称は中国語では **余火**、英語と日本語では **AI Roaster** です。
- Codex、Claude デスクトップアプリ、Claude Code がインストール済みでログインしていれば、現在のアカウントのサブスクリプションと残りの利用枠を読み取ります。検出されない場合は、右上の「設定」で、該当クライアントのパスを確認してください。
- アカウントや利用枠の記録を手動で追加することもできます。Gemini などのプラットフォームは、現時点では手動での更新が必要です。
- 右上のメニューで「システムに合わせる（既定）」、「ライトモード」、「ダークモード」を切り替えられます。
- 現在のアカウントは選択した間隔で自動更新されます。他のアカウントは、それぞれのカードで個別に更新できます。
- Codex または Claude デスクトップアプリのアカウントを切り替えると、該当クライアントが終了して再起動するため、実行中のタスクは中断されます。Claude Code の切り替えは新しいセッションに反映されるため、既存のセッションは開き直してください。
- Claude は同じアカウントの利用枠をデスクトップアプリと Code で共有しますが、認証情報はそれぞれ別に保存します。まず対象のクライアントで通常どおりログインしてください。その後、アカウントカードの「このアカウントを使用」を開き、ダイアログで対象クライアントを選んで認証情報を保存すると、後でそのアカウントに戻せます。
- 対象メールアドレスのログインが失効している（401）と表示された場合は、キャンセルするか、確認後に該当クライアントでログインし直してください。アカウントの本人確認が完了してから、使用中として表示されます。

## データ

インストーラーに開発者のアカウント、認証情報、利用枠の記録は含まれません。利用者のアカウントと認証情報はローカルに保存されます。利用枠の JSON エクスポートには認証ファイルは含まれません。

履歴は既存のスナップショットと、その後に収集したデータから始まります。それ以前の利用状況を補完・捏造することはありません。

## フィードバック

macOS のバージョン、Apple シリコンか Intel か、アプリのバージョン、問題が発生する手順とスクリーンショットを提供してください。アカウントの認証ファイル、キー、データディレクトリ全体は送信しないでください。
